# Ghost — blog, site & paid newsletter (replaces Substack/Wix) -> https://blog.${BASE_DOMAIN}
services:
  db:
    image: mysql:8.0
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${GHOST_DB_ROOT_PASSWORD}
      MYSQL_DATABASE: ghost
      MYSQL_USER: ghost
      MYSQL_PASSWORD: ${GHOST_DB_PASSWORD}
    volumes:
      - db:/var/lib/mysql

  app:
    image: ghost:5-alpine
    restart: unless-stopped
    depends_on:
      - db
    environment:
      url: https://blog.${BASE_DOMAIN}
      # listen on IPv4: hosts with IPv6 disabled fail Ghost's '::' default
      server__host: 0.0.0.0
      database__client: mysql
      database__connection__host: db
      database__connection__user: ghost
      database__connection__password: ${GHOST_DB_PASSWORD}
      database__connection__database: ghost
      mail__transport: SMTP
      mail__options__host: ${SMTP_HOST}
      mail__options__port: ${SMTP_PORT}
      mail__options__auth__user: ${SMTP_USER}
      mail__options__auth__pass: ${SMTP_PASSWORD}
      mail__from: ${SMTP_FROM}
    volumes:
      - content:/var/lib/ghost/content
    networks:
      - default
      - proxy
    labels:
      traefik.enable: "true"
      traefik.http.routers.ghost.rule: Host(`blog.${BASE_DOMAIN}`)
      traefik.http.routers.ghost.entrypoints: websecure
      traefik.http.routers.ghost.tls.certresolver: le
      traefik.http.services.ghost.loadbalancer.server.port: 2368

volumes:
  db:
  content:

networks:
  proxy:
    external: true
