{
  "name": "chasem-landing",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Landing page and phone demo for the Chasem pack for painters",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "qr": "python3 tools/make-qr.py",
    "test": "for t in tests/*.mjs; do echo \"== $t\"; node \"$t\" || exit 1; done"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.80.0",
    "zod": "^4.0.0",
    "pg": "^8.13.0"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.2.0"
  }
}
